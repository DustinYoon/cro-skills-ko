---
name: CRO-forecast
description: |
  이번 분기 숫자·파이프라인이 못 미더울 때 쓴다 — 포캐스트 분류(커밋/베스트/제외)·커버리지와 캐파시티
  수학·컨슘션(사용량) 예측. "얼마나 만들어야 하나", "커버리지가 충분한가"에 수학으로 답한다(상세는 본문).
  Triggers: '포캐스트', 'forecast', '딜 리뷰', 'deal review', '파이프라인 리뷰', '파이프라인 분석', '이번 분기 얼마',
  '커밋', 'commit', '베스트케이스', '포캐스트 정확도', '커버리지', 'coverage', '쿼터 갭', 'quota gap',
  '파이프라인이 충분', '용량 계획', 'capacity', '스테이지 전환', '파이프라인 속도', 'velocity',
  '얼마나 만들어야', '컨슘션', 'consumption', '사용량 기반', 'usage-based', '갭 채우기', '레브옵스', 'RevOps'.
allowed-tools:
  - Read
  - Bash
  - Grep
  - WebFetch
  - Task
  - AskUserQuestion
---

# CRO-forecast — 예측 & 레브옵스 (포캐스트 · 커버리지 · 컨슘션)

이번 분기 숫자를 "영업사원 느낌"이 아니라 **딜별 MEDDPICC 신뢰도 + 커버리지 수학**으로 만든다. 파이프라인은 "느낌"이 아니라 **수학**이다: 목표 대비 얼마가 필요하고, 지금 얼마가 있고, 언제·어디를 채워야 하는지 계산한다. 컨슘션 매출은 시트와 다른 방식으로 예측한다.

> **철칙:** **Economic Buyer 또는 Champion이 고객 측 증거로 검증되지 않은 딜은 커밋/베스트에 넣지 않는다.** 갭이 보이면 '더 열심히'가 아니라 **갭 × (1/승률)**로 답한다. 데이터 없이 쿼터·갭을 지어내지 않는다.

## 참조 (운영체계 먼저 · 프레임워크/코퍼스는 해당 블록에서 로드)
- `~/.claude/skills/CRO/references/revenue-operating-system.md` — MCP/사내정보/persistent context/trust policy/상태 프로토콜. 직접 호출돼도 이 규약을 먼저 적용.
- `~/.claude/skills/CRO/references/frameworks-meddpicc.md` — 포캐스팅 룰(미검증 배제·갭×4·챔피언 동반 미팅·갭 메우기), 갭×4 파이프라인 룰, 6단계 게이트, "시간은 모든 딜을 죽인다".
- `~/.claude/skills/CRO/references/frameworks-fi2i.md` — 파이프라인 생성률(선행 지표), 역할 전문화, 리드 3유형.
- `~/.claude/skills/CRO/references/frameworks-saf.md` — 스테이지별 전환 진단.
- `~/.claude/skills/CRO/references/insights-corpus.md` — **§4 예측 & 레브옵스**(무결정 소멸·컨슘션 리프 변동성·AI 교차검증·캐파시티 vs 생산성·램프를 모델에).
- `~/.claude/skills/CRO/references/data-sources.md` · `mcp-data-access.md` · `conflict-resolution.md` · `context-profile.md`.

## 데이터 소스 (답변 첫 줄에 표기)
1. **MCP** — Salesforce Opportunity(StageName/Amount/CloseDate/Probability/ForecastCategory)로 오픈 딜 전체, OpportunityHistory로 체류일·정체 감지; HubSpot deals 동일. 과거 승률·평균 딜·사이클, 담당자 수·램프.
   - Salesforce 오픈: `SELECT Amount,StageName,CloseDate,OwnerId FROM Opportunity WHERE IsClosed=false`
2. **회사 지식 MCP/코퍼스** — FY plan, 쿼터/보드 목표, forecast policy, stage definition, comp/commit 규칙.
3. **persistent context** — `.cro/context.md`, `.cro/memory/decisions.md`, 과거 forecast 결정/가정.
4. **첨부/입력** — 쿼터, 파이프라인 export(CSV/스크린샷), 램프 가정, 컨슘션 사용 이력.
5. **자문 모드** — 필요한 입력(쿼터·승률·ACV·사이클·담당자·램프)을 질문으로 수집(수치 지어내지 않음).

## 핵심 공식
- **필요 파이프라인 = 목표 매출 ÷ 종합 승률.**
- **커버리지 배수 = 오픈 파이프라인 ÷ 쿼터** (건강 기준 ≈ 승률의 역수; 승률 25%→~4×, MEDDPICC 갭×4와 일치).
- **파이프라인 속도 = (오픈 기회 수 × 승률 × 평균 ACV) ÷ 평균 사이클(일).**
- **용량 = Σ(담당자 × 램프 반영 생산성)** (신입은 램프 3~6개월로 할인).
- **갭 메우기(reconcile) = 갭 × (1/승률)** (승률 25%면 ×4).

## 아티팩트 우선 진입 (저마찰 · 있으면 인테이크 건너뜀)
사용자가 **파이프라인 데이터를 바로 건네면**(export·붙여넣기·URL·파일경로) 인테이크 질문을 생략하고 곧장 파싱→분류/수학으로 간다. `/cro-forecast <아티팩트>` 커맨드도 이 경로.
| 받은 것 | 처리 |
|:--|:--|
| **파이프라인 CSV/export·딜 리스트**(파일경로) | Read로 로드 → 딜별 카테고리·커버리지 계산. |
| **CRM 리포트 URL / 붙여넣기 표** | 붙여넣기면 그대로 파싱, SF/HubSpot URL이면 MCP 조회(`mcp-data-access.md`). |
| **쿼터·목표 숫자**(붙여넣기) | 커버리지·갭 수학의 입력으로 즉시 사용. |
데이터 소스 첫 줄에 무엇을 받았는지 표기(`[첨부:pipeline.csv]`·`[MCP:Salesforce]`). **없는 쿼터·승률은 지어내지 않는다** — 없으면 질문으로 채운다(철칙 유지). 아티팩트가 없으면 아래 라우팅 표로.

## 워크플로 — 진입 라우팅 (어느 블록인가)
| 의도 | 블록 |
|:--|:--|
| "이번 분기 얼마"·딜 리뷰·커밋/베스트 분류 | **A 포캐스트 분류** |
| 커버리지·쿼터 갭·용량·"얼마나 만들어야"·스테이지 병목 | **B 커버리지 수학** |
| 컨슘션/사용량 기반 매출 예측·"리프가 튄다" | **C 컨슘션 포캐스팅** |

> **병렬 처리 (fan-out — 딜이 많을 때만):** 딜 리뷰 대상이 **>8개**면 `Task` 툴로 **딜당 서브에이전트를 병렬** 실행해 처리량·컨텍스트를 확보한다(≤8이면 인라인, 오버헤드 낭비 금지). 서브에이전트는 부모 스킬 본문을 **자동 상속하지 않으므로**, 각 `Task` 프롬프트에 **철칙(통화·CRM에 없는 수치 지어내지 않기)·MEDDPICC 0/1/2 채점 기준(고객 측 증거 기반)·데이터 소스 첫 줄 표기**를 반드시 담는다(또는 "`CRO-forecast` 스킬을 로드해 블록 A 규율로 채점하라"고 지시). 각 서브에이전트는 그 규율 하에 딜당 `{딜명·소스줄·MEDDPICC 0/1/2·카테고리(커밋/베스트/파이프라인/제외)·최상위 갭}`만 구조화 반환한다. 동시성은 ≈6~8로 배치. 반환을 모아 **분류표로 집계**(딜별 소스줄 보존·중복 제거·목표 대비 롤업). 서브에이전트가 못 채운 칸은 '다음 질문'으로 남긴다(집계가 빈칸을 지어내지 않음). 병렬이라고 MEDDPICC 증거 기준을 느슨하게 하지 않는다.

---

## 블록 A — 포캐스트 분류 & 딜 리뷰

1. **오픈 딜 수집** — 딜명·금액·단계·클로즈데이트·오너·마지막 고객 이벤트.
2. **딜별 MEDDPICC 신뢰도 → 카테고리**(`CRO-deal`과 동일 0/1/2):
   - **커밋:** EB 검증(2) + 챔피언 확인 + 임박 컴펠링 이벤트 + 다음 고객 측 이벤트 날짜 확정.
   - **베스트케이스:** 챔피언 있으나 EB 미검증/컴펠링 이벤트 불명확.
   - **파이프라인:** 초기·3 Whys 미완·코치뿐.
   - **제외:** E 또는 C(챔피언) 미검증인데 커밋/베스트로 보고된 딜 → **분기 초 포캐스트에서 뺀다**(가짜면 다른 딜로 채워지고, 진짜면 검증되며 복귀).
3. **목표 대비 갭 + 갭×4** — 커밋·베스트 합계 vs 쿼터. 데이터 없이 쿼터 지어내지 말 것.
4. **리스크 플래그** — EB 미검증 / 다음 고객 이벤트 없음 / 경쟁 기준 이동 / 스테이지 정체(체류일 vs 분기 영업일 ~61일).
5. **갭 메우기 계획(팀 분산)** — 리더 혼자 X. 오너별 갭 분산 + **매니저가 포캐스트 딜 챔피언을 직접 만나는 일정**(분기 첫 달). 제외 딜은 "무엇을 검증하면 복귀"를 명시해 되돌림.
6. **포캐스트 정확도 메모** — 첫 분기 "점", 둘째 "선", 셋째부터 "추세".
- **무결정 경계**(Dixon): 검증된 파이프라인 40~60%가 미결정 소멸 — "빼는 비용 < 잘못 안고 가는 비용."

## 블록 B — 커버리지 & 캐파시티 수학

1. **목표·현황 수집** — 쿼터(팀/개인/분기), 오픈 파이프라인(스테이지별), 과거 승률·ACV·사이클, 담당자·램프.
2. **커버리지 & 갭** — 필요 파이프라인·커버리지 배수·부족분. **미검증 딜은 커버리지에서 할인**(부풀리지 않음).
3. **스테이지 병목**(과거 대비 전환율): 리드→미팅 낮음→발굴(→`CRO-market`/`CRO-coaching`) · 미팅→기회 낮음→디스커버리(→`CRO-deal`) · 기회→클로징 낮음→협상(→`CRO-deal`/`CRO-coaching`).
4. **시간·용량** — 분기 실질 영업일 제한(~61일). 사이클 감안해 **지금 만든 파이프라인이 언제 매출화되는지** 역산; 이번 분기엔 늦으면 다음 분기용 분리.
5. **캐파시티 진단** — **생산적 캐파시티 vs 생산성**(MongoDB), 램프·이탈을 모델에 반영(교체 렙은 6~12개월 사실상 0). 부족하면 채용/램프·역할 전문화 → `CRO-org`.

## 블록 C — 컨슘션(사용량 기반) 포캐스팅

- **시트 라이선스와 근본적으로 다른 계약**(Devavrat Shah) — pay-for-use는 투명성↑.
- **총량은 예측 가능하나 개별 리프(rep/region/계정)는 변동이 크다** — "평균의 함정"에 빠지지 마라. 리프 단위로 커밋을 받지 말고 **코호트·집계 수준**에서 확률적으로 본다.
- **CRM 정형 데이터 → 다신호 AI 교차검증**(사용 이력·계절성·계약 타이밍)으로 이동. 부수효과는 **비난→신뢰 문화**.
- **모션이 추가된다**(MongoDB): 컨슘션은 launch/migrate/select/replace에 "빠른" 5번째 모션을 더함 — 전환은 1.5~2년, 인에이블먼트 재설계 필요(→`CRO-org`). *보상 지급 타이밍(부킹 vs 크레딧 소진)은 `CRO-org`, 프라이싱 설계는 `CRO-market`.*

---

## 출력 형식
1. 데이터 소스 1줄
2. 어느 블록(A~C)
3. 산출물:
   - A: 포캐스트 분류표(딜×카테고리×MEDDPICC 갭) + 커밋·베스트 vs 쿼터 → 갭 → 갭×4 + 리스크 플래그 + 갭 메우기 계획 + 정확도 메모
   - B: 커버리지 계산표(목표/오픈/배수/갭/필요 신규) + 스테이지 병목 + 용량·타이밍 + 채우기 계획
   - C: 컨슘션 예측(집계 vs 리프 변동성) + 신호 목록 + 모션·전환 함의
4. 핸드오프 1줄

## 안티패턴
- ❌ 영업사원 보고 카테고리를 MEDDPICC 근거 없이 신뢰. ❌ 미검증 딜을 "숫자 안 나오니" 커밋에 남기기.
- ❌ 갭 보이는데 갭×4 없이 "더 열심히"로 끝. ❌ 리더 혼자 갭 떠안기. ❌ 데이터 없이 쿼터·갭 지어내기.
- ❌ 스테이지 확률로 부풀린 "가중 파이프라인"을 커버리지로 착각. ❌ 미검증 딜 포함해 커버리지 과대.
- ❌ 램프 무시하고 신입 100% 가정. ❌ 사이클 무시하고 "이번 분기 채우면 됨".
- ❌ 컨슘션을 딜 단위로 억지 커밋(리프 변동성 무시). ❌ "평균의 함정".

## 합리화 차단 (압박 하에서 규율 지키기)
| 이런 생각이 들면 | 현실 |
|:--|:--|
| "영업사원이 커밋이라니 커밋으로 두자" | MEDDPICC 근거 없는 카테고리는 신뢰하지 않는다. **E·C 미검증이면 제외**. |
| "숫자가 안 나오니 이 딜은 남겨 두자" | 빼는 비용 < 잘못 안고 가는 비용. 가짜면 다른 딜로 채워지고 진짜면 복귀. |
| "가중 파이프라인이 크니 커버리지 충분" | 확률 가중을 커버리지로 착각 금지. **미검증 딜은 할인**. |
| "갭 보이지만 더 열심히 하면 되겠지" | **갭 × (1/승률)**로 필요 신규를 계산하고 오너별로 분산. |
| "신입 포함이니 용량은 채워졌어" | 램프(교체 렙 6~12개월 ≈ 0)를 모델에 반영. |

## 핸드오프
개별 딜 블로커·자격심사 → `CRO-deal` · 유입 채널/파이프라인 생성 → `CRO-market` · 확장/컨슘션 계정 성장 → `CRO-account` · 채용·램프·역할 전문화·보상 → `CRO-org` · 반복된 미검증/부풀리기 담당자 → `CRO-coaching`.
